const mysql = require("mysql");
const myconnection = require("express-myconnection");

const dbConfig = {
  host: "localhost",
  user: "root",
  password: "",
  port: "3306",
  database: "db_warteg",
};
